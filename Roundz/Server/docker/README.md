# Service Docker assets

This folder contains Roundz backend assets for service docker assets. The baseline implementation wires these layers through the shared server runtime and Infrastructure manifests.
