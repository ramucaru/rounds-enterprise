import 'package:flutter/material.dart';

class NavigationModuleScreen extends StatelessWidget {
  const NavigationModuleScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('navigation-module')), body: const Center(child: Text('Roundz Rider navigation-module connected module')));
}
