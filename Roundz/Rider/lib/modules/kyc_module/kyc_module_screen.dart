import 'package:flutter/material.dart';

class KycModuleScreen extends StatelessWidget {
  const KycModuleScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('kyc-module')), body: const Center(child: Text('Roundz Rider kyc-module connected module')));
}
