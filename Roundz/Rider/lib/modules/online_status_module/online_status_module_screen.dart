import 'package:flutter/material.dart';

class OnlineStatusModuleScreen extends StatelessWidget {
  const OnlineStatusModuleScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('online-status-module')), body: const Center(child: Text('Roundz Rider online-status-module connected module')));
}
