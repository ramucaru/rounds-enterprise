import 'package:flutter/material.dart';

class NotificationModuleScreen extends StatelessWidget {
  const NotificationModuleScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('notification-module')), body: const Center(child: Text('Roundz Rider notification-module connected module')));
}
