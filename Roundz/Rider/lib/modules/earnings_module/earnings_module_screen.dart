import 'package:flutter/material.dart';

class EarningsModuleScreen extends StatelessWidget {
  const EarningsModuleScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('earnings-module')), body: const Center(child: Text('Roundz Rider earnings-module connected module')));
}
