import 'package:flutter/material.dart';

class TripRequestModuleScreen extends StatelessWidget {
  const TripRequestModuleScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('trip-request-module')), body: const Center(child: Text('Roundz Rider trip-request-module connected module')));
}
