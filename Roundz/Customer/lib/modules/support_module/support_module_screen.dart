import 'package:flutter/material.dart';

class SupportModuleScreen extends StatelessWidget {
  const SupportModuleScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('support-module')), body: const Center(child: Text('Roundz Customer support-module connected module')));
}
