import 'package:flutter/material.dart';

class TripHistoryModuleScreen extends StatelessWidget {
  const TripHistoryModuleScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('trip-history-module')), body: const Center(child: Text('Roundz Customer trip-history-module connected module')));
}
