import 'package:flutter/material.dart';

class BookingModuleScreen extends StatelessWidget {
  const BookingModuleScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('booking-module')), body: const Center(child: Text('Roundz Customer booking-module connected module')));
}
