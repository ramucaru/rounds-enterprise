import 'package:flutter/material.dart';

class ProfileModuleScreen extends StatelessWidget {
  const ProfileModuleScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('profile-module')), body: const Center(child: Text('Roundz Customer profile-module connected module')));
}
