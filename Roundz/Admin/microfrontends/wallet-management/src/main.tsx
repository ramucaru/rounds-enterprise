import React from 'react';
import ReactDOM from 'react-dom/client';
import { WalletManagement } from './App.js';

ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><WalletManagement /></React.StrictMode>);
