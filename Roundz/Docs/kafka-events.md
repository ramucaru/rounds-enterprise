# Kafka Events

Core topics include `roundz.trip.requested`, `roundz.trip.matched`, `roundz.tracking.position.updated`, `roundz.payment.captured`, and `roundz.notification.requested`. Event payloads share the `DomainEvent` envelope from `/Server/shared/common/src/events.ts`.
