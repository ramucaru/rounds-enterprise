# API Documentation

The OpenAPI contract is maintained in `/Shared/contracts/openapi.yaml`. All client applications call the gateway at `/v1/*`; the gateway proxies to backend services using service URLs from environment variables.
